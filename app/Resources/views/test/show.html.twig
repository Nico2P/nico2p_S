{% extends 'base.html.twig' %}

{% block title %} Parametre : {{ slug }} {% endblock %}

{% block body %}

<h1> Le parametre est le suivant : {{ slug }}</h1>

 {# dump() #}

<h3>{{ funFact | raw }}</h3>

<ul>
    {% for note in notes %}
        <li>{{ note }}</li>
    {% endfor %}
</ul>

{% endblock %}